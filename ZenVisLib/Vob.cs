using System.Numerics;

namespace ZenVis.Lib
{
    public class Vob : Visual
    {
        public Vob(string fileName, Vector3 pos, zMAT3 rot3, bool visible) : base(fileName, pos, rot3, visible)
        {
        }
    }
}